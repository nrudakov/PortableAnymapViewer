<Page
    x:Class="Portable_Anymap_Viewer.EditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Portable_Anymap_Viewer"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Pivot Grid.Row="1" SelectionChanged="Pivot_SelectionChanged">
            <PivotItem>
                <PivotItem.Header>
                    <local:TabHeader Label="Edit" Glyph="&#xE70F;" />
                </PivotItem.Header>
                <PivotItem.Content>
                    <TextBox x:Name="EditorText" AcceptsReturn="True" TextWrapping="NoWrap" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" FontFamily="Consolas"/>
                </PivotItem.Content>
            </PivotItem>
            <PivotItem>
                <PivotItem.Header>
                    <local:TabHeader Label="Preview" Glyph="&#xE8FF;" />
                </PivotItem.Header>
                <PivotItem.Content>
                    <canvas:CanvasControl x:Name="EditorCanvas" CreateResources="EditorCanvas_CreateResources" Draw="EditorCanvas_Draw"/>
                </PivotItem.Content>
            </PivotItem>
        </Pivot>

        <CommandBar x:Name="EditorCommandBar" VerticalAlignment="Bottom" Visibility="Visible" Grid.Row="0">
            <CommandBar.Background>
                <SolidColorBrush Color="Black" Opacity="0.1"/>
            </CommandBar.Background>
            <AppBarButton x:Name="EditorCompare" Label="Compare" ToolTipService.ToolTip="Compare (Ctrl+/)" Click="EditorCompare_Click"/>
            <AppBarSeparator/>
            <AppBarButton x:Name="EditorSaveCopy" Icon="SaveLocal" Label="Save copy" ToolTipService.ToolTip="Save copy (Ctrl+S)" Click="EditorSaveCopy_Click"/>
            <AppBarButton x:Name="EditorSave" Icon="Save" Label="Save" ToolTipService.ToolTip="Save" Click="EditorSave_Click"/>
            <AppBarButton x:Name="EditorCancel" Icon="Cancel" Label="Cancel" ToolTipService.ToolTip="Cancel (Esc)" Click="EditorCancel_Click"/>
        </CommandBar>
    </Grid>
</Page>